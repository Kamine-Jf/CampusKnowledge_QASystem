# -*- coding: utf-8 -*-
"""MySQL 数据库基础配置常量定义模块。"""

# MySQL 主机地址，默认连接本地数据库，此处修改为自己的 MySQL 账号密码即可。
MYSQL_HOST = "localhost"

# MySQL 服务端口，通常保持 3306 默认端口，此处修改为自己的 MySQL 账号密码即可。
MYSQL_PORT = 3306

# MySQL 登录用户名，必须具备读取与写入权限，此处修改为自己的 MySQL 账号密码即可。
MYSQL_USER = "root"

# MySQL 登录密码，确保与本地数据库设置一致，此处修改为自己的 MySQL 账号密码即可。
MYSQL_PWD = "123456"

# 默认连接与操作的数据库名称，执行建库脚本后将自动生效，此处修改为自己的 MySQL 账号密码即可。
MYSQL_DB = "campus_qa_db"
